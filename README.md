# IdleFactoryRelease

Release metadata for [Idle Factory](https://github.com/shayanoh/IdleFactory).

`latest.json` holds the latest released version number and store URLs. It is
updated manually whenever a new build is published, and is read by the app
at runtime to check for available updates.
